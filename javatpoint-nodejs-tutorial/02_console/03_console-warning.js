// I can't stand examples that use lower-case constant names, so we're
// just going to upper-case this...
const NAME = 'Garfield';

// Note the backticks to allow reading `NAME` as a variable.
console.warn( `Don't mess with me, ${ NAME }! Don't mess with me!` );
console.log( 'Where is my lasagna??' );
